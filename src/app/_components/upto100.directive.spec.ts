import { Upto100Directive } from './upto100.directive';

describe('Upto100Directive', () => {
  it('should create an instance', () => {
    const directive = new Upto100Directive();
    expect(directive).toBeTruthy();
  });
});
